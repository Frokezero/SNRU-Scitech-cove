# รายงานการวิเคราะห์ระบบ Activity Calendar

จากการตรวจสอบไฟล์ต่างๆ ในระบบ (Backend: `app.py`, Frontend: `script.js`, Database: `*.json`) ผมได้ทำการวิเคราะห์โครงสร้าง การออกแบบ และหาจุดที่อาจเป็นบั๊กหรือข้อควรระวัง เพื่อเตรียมความพร้อมสำหรับการเพิ่มฟังก์ชันในอนาคต ดังนี้ครับ

## 1. โครงสร้างโค้ด (Code Structure)

**Backend (Python/Flask - `app.py`)**
*   **Architecture:** เป็นเว็บแอปพลิเคชันแบบ Monolithic ที่ใช้ Flask เป็น Web Framework ให้บริการทั้งไฟล์ Static (HTML, CSS, JS) และ API (RESTful)
*   **Authentication:** ใช้ระบบ Session ของ Flask (`session['username']`) ในการจัดการการเข้าสู่ระบบ
*   **API Endpoints หลัก:**
    *   `/api/login`, `/api/logout`, `/api/me`: จัดการ Auth
    *   `/api/events` (GET, POST, PUT, DELETE): จัดการข้อมูลกิจกรรม มีการตรวจสอบสิทธิ์ (Role-based) ระหว่าง `admin` (สโมสรนักศึกษา) และ `major` (สาขาวิชา)
    *   `/api/carousel`, `/api/admin/upload-carousel`: จัดการรูปภาพแบนเนอร์
*   **ข้อสังเกต:** มีการประกาศตัวแปร `DATA_FILE = 'events.json'` ซ้ำซ้อนกันในบรรทัดที่ 9 และ 23 (ไม่มีผลกระทบต่อการรัน แต่ควรทำความสะอาดโค้ด)

**Frontend (Javascript - `script.js`)**
*   **Event Fetching & Parsing:** ดึงข้อมูลจาก `/api/events` และพยายามแยกส่วนวันที่ (Parse Date) แบบ String เช่น "24 ม.ค. 69" หรือ "มิ.ย. - ก.ค. 69" โดยใช้ Regular Expression
*   **Dynamic Filtering:** สร้างปุ่มกรอง (Filter) ตาม "เดือน" และ "สาขาวิชา" 
*   **Carousel:** ควบคุมการเลื่อนรูปภาพอัตโนมัติ

---

## 2. การออกแบบระบบและโครงสร้างข้อมูล (System Design & Data Structure)

ระบบใช้ **Flat JSON Files** เป็นฐานข้อมูลแทน Database System (เช่น MySQL/PostgreSQL) เหมาะสำหรับโปรเจกต์ขนาดเล็ก
*   **`users.json`**: เก็บข้อมูลผู้ใช้งานแบบ Dictionary (`"username": {"password": "...", "name": "...", "role": "..."}`) 
    *   *Role ที่มี:* `admin` (จัดการได้ทั้งหมด) และ `major` (จัดการได้เฉพาะของตนเอง)
*   **`events.json`**: เก็บข้อมูลกิจกรรมแบบ Array of Objects
    *   *Schema:* `id` (String/UUID), `date` (String รูปแบบข้อความ), `title` (String), `status` (String), `owner` (String - สาขาที่รับผิดชอบ)
*   **`carousel.json`**: เก็บ Path ของรูปภาพแบนเนอร์แบบ Array of Strings

---

## 3. บั๊ก ความเสี่ยง และจุดที่ต้องปรับปรุง (Bugs & Vulnerabilities)

เพื่อให้ระบบพร้อมสำหรับการพัฒนาต่อหรือนำไปใช้งานจริง นี่คือจุดที่ควรแก้ไข:

### 🔴 ระดับวิกฤต (Critical - ความปลอดภัย)
1.  **รหัสผ่านไม่ได้ถูกเข้ารหัส (Plaintext Passwords):** ในไฟล์ `users.json` รหัสผ่านถูกเก็บเป็นข้อความธรรมดา หากไฟล์หลุดไป รหัสผ่านจะถูกเปิดเผยทั้งหมด (ควรใช้ Hashing เช่น `werkzeug.security.generate_password_hash`)
2.  **ไม่มีการจำกัดประเภทไฟล์อัปโหลด (Unrestricted File Upload):** ใน Route `/api/admin/upload-carousel` ไม่มีการตรวจสอบนามสกุลไฟล์ ผู้ใช้อาจอัปโหลดไฟล์อันตราย (เช่น `.exe`, `.sh`, `.php`) เข้าไปในโฟลเดอร์ `uploads` ได้ ควรเช็ค Allowed Extensions (เช่น อนุญาตเฉพาะ `.png, .jpg, .jpeg, .gif, .webp`)

### 🟡 ระดับกลาง (Medium - ความเสถียรของระบบ)
1.  **ตรรกะการแปลงวันที่ (Date Parsing Logic) เปราะบางมาก:** ใน `script.js` บรรทัดที่ 118 มีการตรวจจับปีด้วย Regex `\b(68|69|70)\b` 
    *   *ปัญหา:* หากผู้ใช้พิมพ์ปีเป็น "2569" แทน "69" หรือในปีพ.ศ. 2571 ระบบจะพังและหาปีไม่เจอ 
    *   *ปัญหา:* มีการ Hardcode `let currentYear = 2569;` ไว้ในบรรทัด 93 ซึ่งพอขึ้นปีหน้า (2570) โค้ดส่วนนี้จะต้องมาแก้ใหม่ด้วยมือ
    *   *วิธีแก้:* ควรให้ Backend เก็บข้อมูลแบบ Timestamp/ISO Date Date (เช่น `2026-05-20`) คู่ไปกับข้อความที่แสดงผล เพื่อให้ Frontend กรองข้อมูลได้ง่ายและแม่นยำ
2.  **Concurrency Issue (Race Condition):** การใช้ฟังก์ชันอ่าน-เขียนไฟล์ JSON (`load_events()` แล้วตามด้วย `save_events()`) หากมีคน 2 คนกดบันทึกข้อมูลพร้อมกันในเสี้ยววินาที ข้อมูลของคนหนึ่งอาจถูกทับและสูญหายได้ (ควรอัปเกรดไปใช้ SQLite หากมีคนใช้งานพร้อมกันหลายคน)

### 🔵 ระดับความสะดวกสบาย (Low - UI/UX & Code Quality)
1.  **Hardcoded Filter List:** รายชื่อสาขาสำหรับการทำปุ่ม Filter ใน `script.js` (ตัวแปร `allBranches`) ถูกพิมพ์ตายตัวไว้ (Hardcode) หากมีการเพิ่มสาขาใหม่ใน `users.json` จะไม่ไปโผล่ในหน้า UI อัตโนมัติ ควรดึงรายชื่อสาขาจาก API หรือดึงจากข้อมูลที่มีอยู่ใน Event
2.  **ไฟล์ `events.js` ซ้ำซ้อน:** มีการสร้างไฟล์ `events.js` ที่เก็บ Array แบบ Hardcode ไว้ ซึ่งดูเหมือนจะไม่ได้ใช้แล้วเนื่องจากตอนนี้ดึงจาก Backend (`events.json`) ถ้าไม่ได้ใช้ควรลบทิ้งเพื่อป้องกันความสับสน
3.  **รูปแบบ ID ไม่สม่ำเสมอ:** ข้อมูลเก่าใช้ ID แบบตัวเลข "1", "2" ส่วนข้อมูลที่สร้างใหม่ใช้ UUID (`uuid.uuid4()`) ไม่มีปัญหาต่อการทำงาน แต่อาจดูไม่สวยงาม

---

**สรุปการเตรียมพร้อม:**
หากคุณต้องการเพิ่มฟังก์ชันใหม่ ผมแนะนำให้เราจัดการ **ตรรกะการอ่านวันที่ใน `script.js`** และ **การเช็คไฟล์อัปโหลด** เป็นอันดับแรกๆ ครับ

คุณต้องการให้ผมเริ่ม **แก้ไขบั๊ก** ตัวไหนก่อน หรือ **ต้องการเพิ่มฟังก์ชันใหม่** อะไรครับ แจ้งได้เลย!
