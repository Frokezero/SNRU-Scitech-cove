# รายงานการวิเคราะห์ระบบและการตรวจสอบ (System Inspection & Diagnostics)

จากการตรวจสอบซอร์สโค้ดปัจจุบันในพื้นที่ทำงาน (Workspace) ทั้งหมด พบว่าระบบได้รับการพัฒนาและอัปเกรดไปอย่างมากจากเวอร์ชันดั้งเดิม (เช่น เปลี่ยนจาก JSON Flat Files มาใช้ **SQLite Database** และอัปเกรด Web Server เป็น **Waitress Production-Grade**) อย่างไรก็ตาม **ยังตรวจพบบั๊กร้ายแรงและข้อผิดพลาดในการเรียกใช้ฟังก์ชัน** ที่จะส่งผลต่อการทำงานของระบบจริง ดังนี้ครับ:

---

## 1. บั๊กที่ตรวจพบในปัจจุบัน (Current Detected Bugs)

### 🔴 ระดับวิกฤต (Critical - ระบบทำงานไม่ได้และแอปพลิเคชันจะล่ม)
#### 1. บั๊ก `part_index` ที่ไม่มีอยู่จริงใน `app.py`
*   **จุดที่เกิดปัญหา:** `app.py` -> ฟังก์ชัน `update_participation_status()` (บรรทัดที่ 1619)
    ```python
    db_save_participation(participations[part_index])
    ```
*   **สาเหตุ:** ฟังก์ชันนี้เพิ่งได้รับการแก้ไขเพื่อเปลี่ยนจากการเขียนไฟล์ JSON ไปเป็นการบันทึกลงฐานข้อมูล SQLite แต่ผู้พัฒนาได้เรียกใช้ตัวแปร `part_index` ซึ่ง **ไม่ได้มีการประกาศหรือนิยามไว้ในฟังก์ชันนี้เลย** (ทำให้เกิด `NameError: name 'part_index' is not defined` ทันทีที่ API ถูกเรียกใช้)
*   **ผลกระทบ:** แอดมิน (Admin) หรือหัวหน้าสาขาวิชา (Major) จะไม่สามารถอนุมัติหรือปฏิเสธหลักฐานการเข้าร่วมกิจกรรมของนักศึกษาผ่านหน้าหลังบ้านได้ ระบบจะคืนค่าสถานะเป็น 500 Server Error
*   **แนวทางการแก้ไข:** เปลี่ยนไปบันทึกข้อมูลตัวแปร `part` โดยตรงภายในวงรอบ `for update in updates` (คือเรียกใช้ `db_save_participation(part)`) และลบโค้ดบรรทัดที่เรียกใช้ `part_index` ด้านนอกลูปออก

---

### 🟡 ระดับปานกลาง (Medium - ข้อมูลไม่แสดงบนหน้าจอ)
#### 2. บั๊กการเรียกคีย์ข้อมูลผิดในหน้า Portfolio (`portfolio.html`)
*   **จุดที่เกิดปัญหา:** `portfolio.html` -> บรรทัดที่ 134-135
    ```javascript
    <td class="text-center">${h(item.date)}</td>
    <td>${h(item.title)}</td>
    ```
*   **สาเหตุ:** หน้าเว็บนี้จะเรียกใช้ข้อมูลจาก API `/api/student/history` ซึ่งส่งค่าผลลัพธ์มาจากตาราง `participations` ในฐานข้อมูลโดยตรง โดยตารางนี้จะใช้คีย์คอลัมน์ชื่อ **`event_date`** และ **`event_title`** (ไม่ใช่ `date` และ `title`)
*   **ผลกระทบ:** เมื่อนักศึกษากดปุ่ม "ดาวน์โหลดแฟ้มสะสมผลงาน (Portfolio PDF)" ระบบจะแสดงผลรายการกิจกรรมในตารางเป็น **ว่างเปล่า หรือ `undefined`**
*   **แนวทางการแก้ไข:** แก้ไขโค้ดใน `portfolio.html` ให้เรียกใช้คีย์ที่ถูกต้อง:
    ```javascript
    <td class="text-center">${h(item.event_date)}</td>
    <td>${h(item.event_title)}</td>
    ```

---

### 🔵 ระดับต่ำ (Low - สคริปต์การทดสอบล้มเหลว)
#### 3. เส้นทาง API ไม่ถูกต้องในไฟล์ทดสอบ `test_concurrency.py`
*   **จุดที่เกิดปัญหา:** `test_concurrency.py` -> ฟังก์ชัน `login_user()` (บรรทัดที่ 41)
    ```python
    res, cookie = make_request("/api/auth/login", {"username": username, "password": password}, 'POST')
    ```
*   **สาเหตุ:** ในไฟล์ทดสอบมีการกำหนด URL ปลายทางของการเข้าสู่ระบบเป็น `/api/auth/login` แต่ในระบบจริงบน `app.py` (บรรทัดที่ 612) กำหนดเส้นทางไว้เป็น `/api/login`
*   **ผลกระทบ:** สคริปต์สำหรับการทำ Stress Test หรือตรวจสอบ Concurrency จะรันไม่ผ่านและแสดงสถานะ 404/401
*   **แนวทางการแก้ไข:** เปลี่ยนเส้นทางใน `test_concurrency.py` จาก `/api/auth/login` ให้เป็น `/api/login`

---

## 2. สิ่งที่ได้รับการปรับปรุงและแก้ไขได้ยอดเยี่ยมแล้ว (Great Improvements Done)

*   **Database Migration:** ระบบเปลี่ยนมาใช้ฐานข้อมูลเชิงสัมพันธ์ **SQLite** พร้อมเปิดใช้งาน **WAL (Write-Ahead Logging)** และ **Synchronous=NORMAL** ช่วยแก้ปัญหา Concurrency (การเขียนชนกันเมื่อมีผู้ใช้เยอะ) ได้อย่างสมบูรณ์แบบ
*   **Security Hardening:**
    *   รหัสผ่านในปัจจุบันถูกเข้ารหัส (Hashed) ด้วย `generate_password_hash` และตรวจสอบด้วย `check_password_hash` ป้องกันกรณีฐานข้อมูลรั่วไหล
    *   มีการจำกัดชนิดไฟล์อัปโหลดรูปภาพอย่างถูกต้องใน `app.py` (`allowed_file` และ `secure_filename`) และจำกัดขนาดของรูปภาพไม่เกิน 5MB
    *   มีการป้องกันสิทธิ์ระดับบทบาท (Role-based Authorization) เช่น `require_role('admin', 'major')`
*   **Production Readiness:** การเลือกใช้ **Waitress** เป็น Web Server ในโหมดโปรดักชันพร้อมกำหนดให้รันได้สูงสุด 12 Threads ทำให้สามารถรองรับโหลดและจำนวนนักศึกษาที่เข้ามาจองกิจกรรมพร้อมกันได้เป็นอย่างดี

---

## 3. แผนการดำเนินการถัดไป (Proposed Next Steps)

เพื่อให้ระบบปฏิทินกิจกรรมคณะวิทยาศาสตร์และเทคโนโลยีทำงานได้อย่างไม่มีสะดุด 100% ผมขอแนะนำให้ดำเนินการแก้ไขข้อผิดพลาดเหล่านี้ทันที:

1.  **[แก้บั๊ก Critical ใน `app.py`]:** ปรับปรุงโครงสร้างของฟังก์ชัน `update_participation_status()` ให้บันทึกข้อมูล `part` ทีละรายการลง SQLite ภายในลูปอย่างถูกต้อง
2.  **[แก้บั๊กใน `portfolio.html`]:** เปลี่ยนคีย์ตัวแปรเป็น `event_date` และ `event_title` เพื่อให้เอกสาร Portfolio PDF แสดงข้อมูลกิจกรรมและวันที่ได้ถูกต้องสวยงาม
3.  **[แก้บั๊กใน `test_concurrency.py`]:** ปรับปรุงเส้นทางการล็อกอินเพื่อให้ไฟล์ทดสอบทำงานได้ถูกต้อง

คุณต้องการให้ผมเริ่ม **ดำเนินการแก้ไขข้อผิดพลาด** เหล่านี้ให้พร้อมใช้งานเลยไหมครับ? หรือมีจุดไหนที่อยากให้ตรวจสอบเพิ่มเติม แจ้งได้ทันทีเลยครับ!
