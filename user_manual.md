# 📖 คู่มือการใช้งานระบบจองกิจกรรมและ LINE Bot (SciTech)

เอกสารนี้ครอบคลุมการใช้งานระบบทั้งหมด ทั้งในส่วนของผู้ใช้งานทั่วไป (นักศึกษา) และผู้ดูแลระบบ (Admin)

---

## 👨‍🎓 1. สำหรับผู้ใช้งานทั่วไป (นักศึกษา)

### 1.1 การใช้งานผ่านหน้าเว็บไซต์หลัก
* **ดูรายการกิจกรรม**: เข้าสู่หน้าแรกของเว็บไซต์เพื่อดูรายการกิจกรรมทั้งหมด สามารถค้นหาตามชื่อกิจกรรม หรือกรองตามหมวดหมู่ได้
* **ดูรายละเอียดและรหัสจองด่วน**: เมื่อคลิกที่กิจกรรม จะปรากฏหน้าต่างรายละเอียด หากกิจกรรมกำลังเปิดรับสมัคร จะมี **"รหัสจองด่วน LINE"** (เช่น `/book 1a2b3c4d`) ปรากฏขึ้น สามารถคัดลอกรหัสนี้ไปวางในแชท LINE Bot เพื่อจองสิทธิ์ได้อย่างรวดเร็ว
* **แก้ไขโปรไฟล์**: หากเข้าสู่ระบบแล้ว สามารถไปที่หน้า "ข้อมูลส่วนตัว" เพื่ออัปเดตข้อมูล (เช่น ชื่อ, สาขา, อีเมล) และสามารถนำ **LINE User ID** ที่ได้จากบอทมาบันทึกในระบบเพื่อรับสิทธิประโยชน์และการแจ้งเตือน

### 1.2 การใช้งานผ่าน LINE Bot
ผู้ใช้สามารถโต้ตอบกับ LINE Bot ผ่านเมนู (Rich Menu) หรือการพิมพ์คำสั่งดังต่อไปนี้:

#### 📱 เมนูลัด (Rich Menu)
* **📅 กิจกรรมวันนี้**: ดูรายการกิจกรรมที่เปิดรับสมัคร
* **⭐ คะแนนสะสม**: เช็คชั่วโมง/คะแนนกิจกรรมสะสมของคุณ
* **📋 ประวัติการจอง**: ดูประวัติการเข้าร่วมกิจกรรม 5 รายการล่าสุด
* **👤 ข้อมูลส่วนตัว**: ดูข้อมูลโปรไฟล์ปัจจุบันที่เชื่อมต่อไว้ (ชื่อ, ชั้นปี, สาขา, คะแนน, อีเมล, LINE ID)
* **❓ วิธีใช้งานบอท**: แสดงหน้าความช่วยเหลือและคำสั่งทั้งหมด
* **🌐 เว็บไซต์หลัก**: เปิดลิงก์ไปยังหน้าเว็บไซต์ระบบจัดการกิจกรรม (ไม่ต้องพิมพ์รหัสผ่านซ้ำหากจำค่าไว้)

#### 💬 คำสั่งแชท (Chat Commands)
* **จองกิจกรรม**: พิมพ์ `/book <รหัสกิจกรรม>` หรือ `จอง <รหัสกิจกรรม>` (เช่น `/book 1a2b3c4d`)
* **ยกเลิกการจอง**: พิมพ์ `ยกเลิก <รหัสกิจกรรม>`
* **เช็คไอดี LINE**: พิมพ์ `ไอดี` หรือ `id` เพื่อรับรหัส LINE User ID สำหรับนำไปกรอกในหน้าเว็บ
* **แก้ไขอีเมล**: พิมพ์ `แก้ไขอีเมล <อีเมลของคุณ>` (เช่น `แก้ไขอีเมล student@snru.ac.th`)
* **แก้ไข LINE ID**: พิมพ์ `แก้ไขไลน์ <LINE ID ใหม่>`
* **ช่วยเหลือ**: พิมพ์ `ช่วย`, `?`, หรือ `คู่มือ` เพื่อเรียกดูวิธีใช้งานเบื้องต้น

---

## 🛠️ 2. สำหรับผู้ดูแลระบบ (Admin)

### 2.1 การเปิดใช้งานเซิร์ฟเวอร์หลัก (Web Server & Webhook)
ระบบพัฒนาด้วยภาษา Python หากต้องการเปิดระบบให้ทำงาน:
1. เปิด Terminal (Command Prompt / PowerShell) ไปที่โฟลเดอร์โปรเจกต์ `d:\โฟลเดอร์ใหม่ (6)\1122220`
2. รันคำสั่งเปิดเซิร์ฟเวอร์:
   ```powershell
   .venv\Scripts\python.exe app.py
   ```
   *(ระบบจะทำงานที่พอร์ต 5000)*
3. **สำคัญสำหรับการเชื่อมต่อ LINE:** หากรันบนเครื่องคอมพิวเตอร์ส่วนตัว (Local) จะต้องเปิดใช้งาน `ngrok` คู่กันเสมอ เพื่อทำการเปิดพอร์ตออกสู่โลกอินเทอร์เน็ตด้วยคำสั่ง:
   ```powershell
   ngrok http 5000
   ```
   *(นำ URL https ที่ได้จาก ngrok ไปอัปเดตใน LINE Developers Webhook URL เสมอ)*

### 2.2 การติดตั้งและอัปเดตหน้าตา LINE Rich Menu
หากมีการแก้ไขโค้ดรูปภาพเมนู หรือมีการเปลี่ยน URL ของเว็บไซต์ (เช่น ngrok เปลี่ยนแปลง) ให้ทำการรันสคริปต์นี้เพื่อสร้างภาพเมนูใหม่และติดตั้งไปยังมือถือของผู้ใช้ทุกคนโดยอัตโนมัติ:
```powershell
.venv\Scripts\python.exe setup_rich_menu.py
```
> [!NOTE]  
> สคริปต์ถูกออกแบบมาให้ค้นหา URL ของ ngrok อัตโนมัติ (หากเปิดโปรแกรมงทิ้งไว้) จากนั้นจะนำ URL ไปฝังในปุ่ม "เว็บไซต์หลัก" ให้เอง

### 2.3 การส่งข้อความประกาศ (LINE Broadcast)
คุณสามารถส่งข่าวสาร แจ้งเตือน หรือประชาสัมพันธ์กิจกรรมไปยัง **ผู้ติดตาม LINE ทุกคน** ในคราวเดียวได้โดย:
1. รันสคริปต์:
   ```powershell
   .venv\Scripts\python.exe broadcast_to_all.py
   ```
2. พิมพ์ข้อความที่ต้องการส่ง (สามารถกด Enter เพื่อขึ้นบรรทัดใหม่ได้เรื่อยๆ)
3. เมื่อเขียนข้อความเสร็จแล้ว ให้ขึ้นบรรทัดใหม่แล้วพิมพ์คำว่า `SEND` (ตัวพิมพ์ใหญ่) จากนั้นกด Enter เพื่อจบการเขียน
4. ระบบจะพิมพ์สรุปข้อความให้ตรวจสอบอีกครั้ง ให้พิมพ์ `y` เพื่อยืนยันคำสั่งยิงบรอดแคสต์

### 2.4 การจัดการฐานข้อมูล (Database)
ระบบใช้ SQLite เป็นฐานข้อมูลหลัก ข้อมูลทั้งหมดจะถูกเก็บในไฟล์ **`database.db`**:
* **users**: เก็บข้อมูลนักศึกษา (ชื่อ, สาขา, ชั้นปี, คะแนน, รหัสผ่าน, อีเมล, LINE ID)
* **activities**: เก็บรายการข้อมูลกิจกรรม (ชื่องาน, วันที่, สถานที่)
* **registrations**: เก็บประวัติการจองและการเข้าร่วมกิจกรรมของนักศึกษา

> [!WARNING]  
> ไฟล์ `database.db` ถูกปกปิดการตรวจสอบด้วย Git (`.gitignore`) เรียบร้อยแล้ว เพื่อป้องกันปัญหาการอัปโหลดข้อมูลส่วนตัวของผู้ใช้ขึ้นสู่ Public GitHub แนะนำให้แอดมินทำการ **Copy ไฟล์ฐานข้อมูลนี้เก็บเป็น Backup ด้วยตนเองอย่างสม่ำเสมอ**
